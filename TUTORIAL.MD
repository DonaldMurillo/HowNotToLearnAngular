# 000 - HOW TO (NOT) GET STARTED
## NOT INSTRUCTIONS:
- cd into tutor-app folder
- `npm install` (npm i)
- from there run `ng serve`
- check around the folder structure

## THINGS (NOT) TO READ ABOUT:
- What is a component?
- Why use components?
- Angular modules
- SPA (Single Page Applications)